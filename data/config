
# v360p, v480p, best_av, list_format
-f bestvideo[height=360]+bestaudio
-f bestvideo[height=480]+bestaudio
-f bestvideo[ext=mp4]+bestaudio[ext=m4a]/bestvideo+bestaudio' --merge-output-format mp4
--list-formats

video_format -f bestvideo[height=360]+bestaudio --check-formats 

# no_ID, w_ID
title_format w_ID

# no_index, w_index, rev_index
index_format no_index

# no_folder, w_folder
folder_format no_folder

# no_sponsor, w_ponsor
sponsor_block no_sponsor

# subtitle language
sub_lang en

# subtitle type
# --write-sub, --write-auto-sub
sub_type --write-auto-sub --convert-subs=srt --use-postprocessor srt_fix
